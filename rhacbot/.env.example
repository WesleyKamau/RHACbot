# Frontend environment variables (copy to .env.local for local development)
# Do NOT store secrets here.

# Optional: full API base URL (overrides the defaults below)
# REACT_APP_API_URL=http://127.0.0.1:5000

# Optional: API prefix (defaults to '/api' in development and '' in production)
# REACT_APP_API_PREFIX=/api

# Optional: custom basename for Router if your app is served from a subpath
# REACT_APP_BASENAME=/RHACbot
